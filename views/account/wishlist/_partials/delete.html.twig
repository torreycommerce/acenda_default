{% set result = api.delete('/wishlist/' ~ wishlist.id) %}
{% if result %}
    {% do app.user.setFlash('Deleted wish list successfully.') %}
{% else %}
    {% do app.user.setFlash('Error deleting wish list.', 'danger') %}
{% endif %}
{% do app.redirect('/account/wishlists') %}
